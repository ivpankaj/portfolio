// tailwind.config.ts
const tailwindConfig = {
    darkMode: "class",
    content: [
      "./src/**/*.{js,ts,jsx,tsx,mdx}",
      // add other paths if needed
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  };

export default tailwindConfig;
  